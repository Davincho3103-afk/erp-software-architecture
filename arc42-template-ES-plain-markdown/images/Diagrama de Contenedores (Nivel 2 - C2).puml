@startuml

actor "Administrador de Compras" as admin

rectangle "Sistema ERP" {
  rectangle "SPA Web (React)" as spa
  rectangle "API Backend (Spring Boot)" as api
  database "Base de Datos (PostgreSQL)" as db

  spa --> api : HTTPS / JSON
  api --> db : JDBC
}

admin --> spa : Usa el sistema (HTTPS)

@enduml
